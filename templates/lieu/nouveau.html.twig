{% extends 'base.html.twig' %}

{% block title %}Nouveau lieu{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <h1 class="mb-4">➕ Ajouter un lieu</h1>

        {{ form_start(form) }}
        <div class="card shadow-sm">
            <div class="card-body">
                <div class="mb-3">
                    {{ form_label(form.nom) }}
                    {{ form_widget(form.nom) }}
                    {{ form_errors(form.nom) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.adresse) }}
                    {{ form_widget(form.adresse) }}
                    {{ form_errors(form.adresse) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.ville) }}
                    {{ form_widget(form.ville) }}
                    {{ form_errors(form.ville) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.capacite) }}
                    {{ form_widget(form.capacite) }}
                    {{ form_errors(form.capacite) }}
                </div>
            </div>
        </div>
        <div class="mt-3 d-flex gap-2">
            <button type="submit" class="btn btn-success">✅ Créer le lieu</button>
            <a href="{{ path('app_lieux_liste') }}" class="btn btn-outline-secondary">← Annuler</a>
        </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}